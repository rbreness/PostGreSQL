#In this SQL code, I'm querying a database that's holding Nexflix data to answer questions about the data.

#1 How many movie titles are there in the database? (movies only, not tv shows)
SELECT COUNT(title) FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info"
WHERE type='Movie';

#2 When was the most recent batch of tv shows and/or movies added to the database?
SELECT date_added FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info" 
    ORDER BY date_added desc;

#2 List all the movies and tv shows in alphabetical order.
SELECT title FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info" 
    ORDER BY title ASC;
    
#3 What is the oldest movie in the database and what year was it made?
SELECT title,release_year FROM "CharlotteChaze/BreakIntoTech"."netflix_titles_info"
    ORDER BY release_year
    LIMIT 1;
    
#4 Who was the Director for the movie Bright Star?
SELECT "CharlotteChaze/BreakIntoTech"."netflix_people".director
, "CharlotteChaze/BreakIntoTech"."netflix_titles_info".title
FROM "CharlotteChaze/BreakIntoTech"."netflix_people"
JOIN "CharlotteChaze/BreakIntoTech"."netflix_titles_info"
ON "CharlotteChaze/BreakIntoTech"."netflix_people".show_id =
"CharlotteChaze/BreakIntoTech"."netflix_titles_info".show_id
WHERE title='Bright Star';    
